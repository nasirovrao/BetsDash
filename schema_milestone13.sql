-- Milestone 13: банк/прирост банка на ПУБЛИЧНОМ профиле — теперь скрыт по
-- умолчанию. Владелец включает показ вручную в настройках профиля (обычно
-- только на время конкретного челленджа), остальные показатели (винрейт,
-- профит, ROI и т.д.) видимость не меняют — речь только про 2 карточки
-- "Банк" и "Прирост банка".
--
-- Требует применённого schema_milestone7.sql (таблица profiles).

alter table public.profiles add column if not exists show_bank boolean not null default false;
